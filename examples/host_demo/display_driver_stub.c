/* Stands in for a real src/display_driver.c (human/vendor-owned, never
 * regenerated — see architecture.md Stage 8). The host mock driver
 * (runtime/embedded_c/host_mock/mock_driver.c) needs no hardware init.
 */
void display_driver_init(void) {}
