screens:
  - pwm.screen.yaml
  - bus_status.screen.yaml
  - relay.screen.yaml
nav:
  kind: tabs
  targets:
    - { screen: PWM, title: "PWM" }
    - { screen: BusStatus, title: "SERIAL" }
    - { screen: Relay, title: "Output" }
display:
  size: { w: 320, h: 480 }
  color: rgb565
  bus: parallel
input:
  modality: encoder
