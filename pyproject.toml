[project]
name = "janus"
version = "0.0.1"
description = "Pre-harpia GUI schema + code generator"
requires-python = ">=3.10"
dependencies = ["PyYAML"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["janus*"]
